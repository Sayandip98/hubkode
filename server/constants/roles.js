const APP_ROLES = {
  USER: "user",
  ADMIN: "admin",
};

const ORG_ROLES = {
  OWNER: "owner",
  MEMBER: "member",
};

const REPO_ROLES = {
  READ: "read",
  WRITE: "write",
  ADMIN: "admin",
};

export { APP_ROLES, ORG_ROLES, REPO_ROLES };
